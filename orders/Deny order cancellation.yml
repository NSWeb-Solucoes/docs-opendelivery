info:
  name: Deny order cancellation
  type: http
  seq: 6
  tags:
    - orders

http:
  method: POST
  url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
  params:
    - name: orderId
      value: "{{order_id}}"
      type: path
  body:
    type: json
    data: |-
      {
        "reason": "Motivo",
        "code": "DISH_ALREADY_DONE"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status code is a documented response", function () {
          expect([204, 400, 401, 404, 422]).to.include(res.getStatus());
        });

        if (res.getStatus() === 204) {
          test("204: denies order cancellation with no content", function () {
            const body = res.getBody();
            expect(body === undefined || body === null || body === '').to.be.true;
          });
        }

        if (res.getStatus() === 400) {
          test("400: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 400);
          });
        }

        if (res.getStatus() === 401) {
          test("401: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 401);
          });
        }

        if (res.getStatus() === 404) {
          test("404: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 404);
          });
        }

        if (res.getStatus() === 422) {
          test("422: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 422);
          });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: No Content
    request:
      url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
      method: POST
      params:
        - name: orderId
          value: "{{order_id}}"
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "Motivo",
            "code": "DISH_ALREADY_DONE"
          }
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
      method: POST
      params:
        - name: orderId
          value: "{{order_id}}"
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "Motivo",
            "code": "DISH_ALREADY_DONE"
          }
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"orderId inv\u00e1lido","status":400}'
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
      method: POST
      params:
        - name: orderId
          value: "{{order_id}}"
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "Motivo",
            "code": "DISH_ALREADY_DONE"
          }
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"Unauthorized","status":401}'
  - name: 404 Response
    description: Not Found
    request:
      url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
      method: POST
      params:
        - name: orderId
          value: "{{order_id}}"
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "Motivo",
            "code": "DISH_ALREADY_DONE"
          }
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"Pedido n\u00e3o encontrado","status":404}'
  - name: 422 Response
    description: Unprocessable Entity
    request:
      url: "{{baseUrl}}/v1/orders/:orderId/denyCancellation"
      method: POST
      params:
        - name: orderId
          value: "{{order_id}}"
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "Motivo",
            "code": "DISH_ALREADY_DONE"
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"Nenhuma solicita\u00e7\u00e3o de cancelamento pendente","status":422}'

docs: |-
  Informs the **Ordering Application** that the cancellation request has been denied and the order cannot be Cancelled.

  > It is expected to receive a `CANCELLATION_DENIED` event after this action.

  <br /><br />
  <span style="text-decoration:underline; color:#26323880; font-size:13.006px">HOST:</span> **ORDERING APPLICATION**

  <span style='text-decoration:underline; color:#26323880; font-size:13.006px'>DIRECTION:</span>
  <div align='center'>
    <img src='https://abrasel-nacional.github.io/opendelivery/images/directionPOSTSS_OA.png' />
  </div>
