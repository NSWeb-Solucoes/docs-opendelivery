info:
  name: Get Merchant Status and Service Hours
  type: http
  seq: 2
  tags:
    - merchant

http:
  method: GET
  url: "{{baseUrl}}/v1/merchant/:id/status"
  params:
    - name: id
      value: "{{merchant_id}}"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("status code is a documented response", function () {
          expect([200, 401, 404]).to.include(res.getStatus());
        });

        if (res.getStatus() === 200) {
          test("200: returns merchant status and service hours", function () {
            expect(res.getHeader('content-type')).to.include('application/json');
            const body = res.getBody();
            expect(body).to.be.an('object');
            expect(body).to.have.property('status').that.is.a('string');
            expect(body).to.have.property('services').that.is.a('array');
            expect(body).to.have.property('orderingAppMerchantId').that.is.a('string');
          });
        }

        if (res.getStatus() === 401) {
          test("401: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 401);
          });
        }

        if (res.getStatus() === 404) {
          test("404: returns problem+json error", function () {
            expect(res.getHeader('content-type')).to.include('application/problem+json');
            const body = res.getBody();
            expect(body).to.have.property('title').that.is.a('string');
            expect(body).to.have.property('status', 404);
          });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/merchant/:id/status"
      method: GET
      params:
        - name: id
          value: "{{merchant_id}}"
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "status": "AVAILABLE",
            "services": [
              {
                "id": "service-16",
                "serviceType": "DELIVERY",
                "serviceHours": [
                  {
                    "weekHours": [
                      {
                        "dayOfWeek": [
                          "SUNDAY",
                          "MONDAY",
                          "TUESDAY",
                          "WEDNESDAY",
                          "THURSDAY",
                          "FRIDAY",
                          "SATURDAY"
                        ],
                        "timePeriods": {
                          "startTime": "00:00:00.000Z",
                          "endTime": "23:59:00.000Z"
                        }
                      }
                    ],
                    "holidayHours": []
                  }
                ]
              },
              {
                "id": "service-31",
                "serviceType": "DELIVERY",
                "serviceHours": [
                  {
                    "weekHours": [
                      {
                        "dayOfWeek": [
                          "SUNDAY",
                          "MONDAY",
                          "TUESDAY",
                          "WEDNESDAY",
                          "THURSDAY",
                          "FRIDAY",
                          "SATURDAY"
                        ],
                        "timePeriods": {
                          "startTime": "00:00:00.000Z",
                          "endTime": "23:59:00.000Z"
                        }
                      }
                    ],
                    "holidayHours": []
                  }
                ]
              },
              {
                "id": "service-32",
                "serviceType": "DELIVERY",
                "serviceHours": [
                  {
                    "weekHours": [
                      {
                        "dayOfWeek": [
                          "SUNDAY",
                          "MONDAY",
                          "TUESDAY",
                          "WEDNESDAY",
                          "THURSDAY",
                          "FRIDAY",
                          "SATURDAY"
                        ],
                        "timePeriods": {
                          "startTime": "00:00:00.000Z",
                          "endTime": "23:59:00.000Z"
                        }
                      }
                    ],
                    "holidayHours": []
                  }
                ]
              }
            ],
            "orderingAppMerchantId": "123e4567-e89b-412d-a456-426614174000"
          }
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/v1/merchant/:id/status"
      method: GET
      params:
        - name: id
          value: "{{merchant_id}}"
          type: path
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"Unauthorized","status":401}'
  - name: 404 Response
    description: Not Found
    request:
      url: "{{baseUrl}}/v1/merchant/:id/status"
      method: GET
      params:
        - name: id
          value: "{{merchant_id}}"
          type: path
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/problem+json
      body:
        type: json
        data: '{"title":"Merchant n\u00e3o encontrado","status":404}'

docs: |-
  Returns the merchant status, service hours (serviceHours), and sourceAppId.
  <br /><br />
  <span style="text-decoration:underline; color:#26323880; font-size:13.006px">HOST:</span> **SOFTWARE SERVICE**

  <span style="text-decoration:underline; color:#26323880; font-size:13.006px">DIRECTION:</span>
  <div align="center">
    <img src="https://abrasel-nacional.github.io/opendelivery/versions/rc/images/directionGETSS_OA.png" />
  </div>
